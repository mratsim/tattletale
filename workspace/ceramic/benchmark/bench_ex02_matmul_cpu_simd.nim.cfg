# Force release + danger for meaningful benchmarks
-d:release
-d:danger
--opt:speed

# Enable SIMD micro-kernel dispatch on x86-64
@if amd64:
  -d:TTT_SIMD_ARCH=avx_fma
  --passC:"-mavx -mfma -march=native"
@else:
  --passC:"-march=native"
@end
