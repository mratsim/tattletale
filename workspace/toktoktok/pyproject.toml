[project]
name = "toktoktok"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tokenizers>=0.20.0",
    "tiktoken>=0.12.0",
    "tokendagger>=0.1.0",
]

[tool.uv]
package = false
